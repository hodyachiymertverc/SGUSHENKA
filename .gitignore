# Настоящие секреты — не должны попадать в публичный репозиторий
# на GitHub. Шаблоны с плейсхолдерами (*.example.js) остаются
# в репозитории и коммитятся как обычно.
# Подробности и инструкция по деплою — в SETUP-SECRETS.md
firebase-config.js
admin-config.js

# системные файлы
.DS_Store
Thumbs.db

# зависимости сборки TypeScript (см. package.json/tsconfig.json) —
# не коммитим, ставятся заново через `npm ci` (и то же самое делает
# шаг сборки в .github/workflows/deploy-pages.yml перед деплоем)
node_modules/
