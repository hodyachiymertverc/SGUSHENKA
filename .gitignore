# Настоящие секреты — не должны попадать в публичный репозиторий
# на GitHub. Шаблоны с плейсхолдерами (*.example.js) остаются
# в репозитории и коммитятся как обычно.
# Подробности и инструкция по деплою — в SETUP-SECRETS.md
firebase-config.js
admin-config.js

# системные файлы
.DS_Store
Thumbs.db
