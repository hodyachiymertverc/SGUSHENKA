/* =========================================================
   ШАБЛОН для admin-config.js — этот файл (.example.js) можно
   спокойно коммитить в GitHub, в нём нет настоящего пароля.

   Как пользоваться:
   1. Скопируй этот файл и назови копию admin-config.js
      (без .example) — он уже в .gitignore, поэтому не попадёт
      в репозиторий.
   2. Вставь свой пароль вместо "СВОЙ_ПАРОЛЬ_СЮДА".
   3. Если сайт разворачивается через GitHub Actions (см.
      SETUP-SECRETS.md) — этот шаг делать не нужно, файл
      соберётся автоматически из секрета ADMIN_PASSWORD.
========================================================= */
window.ADMIN_PASSWORD = "СВОЙ_ПАРОЛЬ_СЮДА";
